{
  "name": "tuner",
  "version": "1.0.0",
  "description": "A tuning app",
  "type": "module",
  "scripts": {
    "start" : "node esbuild.config.js -w",
    "build": "node esbuild.config.js",
    "test": "jest --config jest.config.js --no-cache --runInBand --collect-coverage"
  },
  "author": "Tim Creasman",
  "license": "ISC",
  "dependencies": {
    "pitchy": "^2.0.3",
    "sdf-2d": "^0.7.6"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "esbuild": "^0.13.7",
    "esbuild-jest": "^0.5.0",
    "esbuild-serve": "^1.0.1",
    "jest": "^27.2.5"
  }
}
