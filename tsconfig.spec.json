{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Spec files should be allowed to be isolated modules:
    //    "isolatedModules": false,
    "types": [
      "mocha"
    ]
  },
  "include": [
    "src/**/__tests__/*"
  ]
}
