import {TunerComponent} from '../tuner.component';

describe('TunerComponent', function () {

    // const component: TunerComponent;

    describe('tuner body', function () {
        let tunerBody: Element;

        before(() => {
            // tunerBody = fixture
        });

        it('should resume audio context when clicked', async () => {
            // tunerBody.
        });
    });
});
